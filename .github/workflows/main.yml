name: Build and deploy

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:latest
        env:
          POSTGRES_DB: ${{ DB_TEST_CONFIG.DB_NAME }}
          POSTGRES_USER: ${{ DB_TEST_CONFIG.DB_USER }}
          POSTGRES_PASSWORD: ${{ DB_TEST_CONFIG.DB_PASSWORD }}
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U $POSTGRES_USER"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Instalar python
        uses: actions/setup-python@v2
        with:
          python-version: "3.12"

      - name: Instalar dependências
        run: |
          python -m venv .venv
          . .venv/bin/activate
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run tests
        env:
          DB_USER: ${{ DB_TEST_CONFIG.DB_USER }}
          DB_PASSWORD: ${{ DB_TEST_CONFIG.DB_PASSWORD }}
          DB_HOST: ${{ DB_TEST_CONFIG.DB_HOST }}
          DB_NAME: ${{ DB_TEST_CONFIG.DB_NAME }}
        run: |
          . .venv/bin/activate
          python -m pytest case_tests.py
